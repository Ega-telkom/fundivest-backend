# .air-worker.toml
root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/worker"
  stop_on_error = true
  cmd = "go build -o ./tmp/worker ./cmd/worker"
  exclude_regex = ["_test.go"]
  include_ext = ["go", "html"]